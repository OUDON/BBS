.post{ id: "post-#{post.id}" }
  .post-header
    %span= "#{ post.in_topic_id }: "
    %span= post.user.name
    %span= post.created_at
  .post-content.lead
    = simple_format(post.content)
  <hr />
